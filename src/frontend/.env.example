# scepa-rs metadata server: the browser calls its /metadata endpoints
# directly (extraction, retrieval, saving edited fields), authenticated via
# an Authentik session cookie set by that server's own /auth/login flow.
VITE_SCEPA_METADATA_URL=http://localhost:8081
